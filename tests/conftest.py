import logging
import pytest
from _pytest.runner import CallInfo


#pytest.fixture(scope="function", autouse=True)
#ef pytest_exception_interact(call: CallInfo, report):
#   logger = logging.getLogger(__name__)
#   if report.failed:
#       logger.error(call.excinfo)
